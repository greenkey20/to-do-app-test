# 서버 환경 설정 정보
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/db_todo
    username: todo # TODO 데이터베이스 접근 정보 숨겨야(환경변수 등) 하나..?
    password: mysql
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
